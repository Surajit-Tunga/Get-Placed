CREATE DATABASE IF NOT EXISTS NEW_COL;
USE NEW_COL;

-- Crate two tables

CREATE TABLE STUDENT (
    ID INT PRIMARY KEY,
    NAME VARCHAR(50),
    DEPT_ID INT
);

CREATE TABLE DEPARTMENT (
    ID INT PRIMARY KEY,
    NAME VARCHAR(50)
);

INSERT INTO STUDENT VALUES
(1, 'Rahul', 1),
(2, 'Priya', 2),
(3, 'Amit', 1),
(4, 'Rohan', 3);

INSERT INTO DEPARTMENT VALUES
(1, 'CSE'),
(2, 'ECE'),
(4, 'ME');

-- INNER JOIN

